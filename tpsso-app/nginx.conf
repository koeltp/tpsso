server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由支持（OIDC 客户端纯前端，无 API 代理）
    location / {
        try_files $uri $uri/ /index.html;
    }
}